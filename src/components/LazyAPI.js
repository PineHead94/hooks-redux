export default function LazyAPI() {
  return (
    <div>
      <div className="lazy-main-container">
        Lazy loading test, doesn't works with data fetching, only used for
        optimizing component loading.
      </div>
    </div>
  );
}
